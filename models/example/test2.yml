version: 2

models:
    - name: Stg_Prod_Test2
      description: "It will store the Prod Info"
      columns:
          - name: PRODUCT_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null